{
  "name": "mern-azure-checkpoint",
  "version": "1.0.0",
  "description": "MERN stack app deployed on Microsoft Azure",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client && cp -r client/build/. server/public/",
    "start": "node server/server.js",
    "dev-server": "npm run dev --prefix server",
    "dev-client": "npm start --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
